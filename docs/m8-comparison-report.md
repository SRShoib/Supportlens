# M8 comparison report: dense retrieval vs dense + cross-encoder rerank

Generated by `scripts/generate_m8_report.py` from `eval_runs` rows persisted during this run -- every number below comes from a committed eval run (CLAUDE.md rule #5), nothing here is hand-typed.

## Retrieval hit-rate@5

100-query synthetic eval set (`ml/data/retrieval_eval_set.py`): each query is a resolved ticket's first customer message, the known-relevant answer is that ticket's own id. See `docs/decisions.md` for why this is a documented limitation (not real held-out user queries), same shortcut class as this project's other weak-supervision steps.

| Variant | hit-rate@5 | Queries |
|---|---|---|
| dense only (`dense_v1`) | 0.900 | 100 |
| dense + rerank (`dense_rerank_v1`) | 0.920 | 100 |

### Rerank vs no-rerank

**Reranking helps**: hit-rate@5 rises from **0.900** (dense only) to **0.920** (dense + cross-encoder), a delta of **+0.020**, above the 0.02 margin treated as a real difference here.

## Example queries

| Query | Dense hit? | Dense+rerank hit? |
|---|---|---|
| Since I updated the software of mi iPhone a turtle is faster <USER> | yes | yes |
| <USER> ridiculous policy of charging $150 to restate miles when you book usin... | yes | yes |
| <USER> surprised me with the ever best customer centric approach. It's much b... | no | no |
| <USER> have you in the space of a week discontinued your giant couscous w/but... | yes | yes |
| <USER> "Us setup cartridges" keeps showing up on my printer...what does this ... | yes | yes |
| Hey <USER>, why does FlyFi block VPN access? That's mighty inconvenient. Woul... | yes | yes |
| #BreakingNews: <USER> bans "smart bags" / "smart luggage" from checked bags: ... | yes | yes |
| <USER> please check, deactivate any subscription, beside the data subscriptio... | yes | yes |
