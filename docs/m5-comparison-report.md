# M5 comparison report: sentiment/emotion transformer fine-tunes vs classical baselines

Generated by `scripts/generate_m5_report.py` from `eval_runs` rows persisted during this run -- every number below comes from a committed eval run (CLAUDE.md rule #5), nothing here is hand-typed.

## Sentiment

### Accuracy (test macro-F1)

| Model | Macro-F1 |
|---|---|
| baseline (`baseline_logistic_regression_v1`) | 0.6193 |
| transformer (`transformer_distilbert-base-uncased_v1`) | 0.6835 |

### Latency (CPU, single request, p50/p95)

| Model | p50 ms | p95 ms |
|---|---|---|
| baseline | 1.7 | 1.9 |
| distilbert-base-uncased | 12.6 | 16.6 |

### Export size

| Model | Size |
|---|---|
| baseline | 3.4 MB |
| distilbert-base-uncased | 256.3 MB |

### Recommendation

**Deploy the transformer** (`transformer_distilbert-base-uncased_v1`). Macro-F1 delta over the baseline is **+0.0643**, above the 0.02 margin treated as a real win here — worth the 74x size and 7.4x latency cost.

## Emotion

### Accuracy (test macro-F1)

| Model | Macro-F1 |
|---|---|
| baseline (`baseline_linear_svc_v1`) | 0.6541 |
| transformer (`transformer_distilbert-base-uncased_v1`) | 0.7598 |

### Latency (CPU, single request, p50/p95)

| Model | p50 ms | p95 ms |
|---|---|---|
| baseline | 1.7 | 2.1 |
| distilbert-base-uncased | 11.6 | 13.0 |

### Export size

| Model | Size |
|---|---|
| baseline | 2.6 MB |
| distilbert-base-uncased | 256.3 MB |

### Recommendation

**Deploy the transformer** (`transformer_distilbert-base-uncased_v1`). Macro-F1 delta over the baseline is **+0.1057**, above the 0.02 margin treated as a real win here — worth the 100x size and 6.6x latency cost.

## Real-ticket trajectories (qualitative)

tweet_eval's test-set accuracy above proves head quality; this shows the trajectory feature itself (SPEC M5's actual deliverable) on real ingested tickets, computed by `scripts/compute_sentiment_trajectories.py`:

| Ticket | Sequence | Ending | Resolution quality |
|---|---|---|---|
| `aadf1b25-eb9b-528d-8f1b-f688eb758407` | positive → positive | positive | +0.864 |
| `9f40ba8e-0062-5179-8c58-7401f0fb0ae6` | negative → positive | negative | -0.496 |
| `4861fd06-2bca-53ff-b1b0-03874d73274a` | neutral → positive → negative → positive → positive → neutral → neutral → neutral → negative → negative → neutral → neutral | neutral | +0.000 |
| `f6a2f576-3f04-5c23-8f96-706894faa7db` | negative → neutral | negative | -0.319 |
| `48c2a742-bd9b-59d2-9f81-0150a9425145` | neutral → neutral → neutral | neutral | +0.000 |
