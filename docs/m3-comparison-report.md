# M3 comparison report: transformer fine-tunes vs classical baselines

Generated by `scripts/generate_m3_report.py` from `eval_runs` rows persisted during this run plus the M2 baseline `eval_runs` rows — every number below comes from a committed eval run (CLAUDE.md rule #5), nothing here is hand-typed.

## Intent

### Accuracy (test macro-F1)

| Model | Macro-F1 |
|---|---|
| baseline (`baseline_linear_svc_v1`) | 0.9990 |
| transformer (`transformer_distilbert-base-uncased_v1`) | 0.9975 |
| transformer (`transformer_deberta-v3-small_v1`) | 0.9970 |

### Latency (CPU, single request, p50/p95)

| Model | p50 ms | p95 ms |
|---|---|---|
| baseline | 1.6 | 1.7 |
| distilbert-base-uncased | 11.3 | 13.7 |
| deberta-v3-small | 30.0 | 33.0 |

### Export size

| Model | Size |
|---|---|
| baseline | 3.4 MB |
| distilbert-base-uncased | 256.4 MB |
| deberta-v3-small | 552.0 MB |

### Recommendation

**Keep the baseline.** Macro-F1 delta is only **-0.0015** — within the 0.02 margin treated as noise, not a real win. The transformer (`transformer_distilbert-base-uncased_v1`) costs 75x the size and more latency for accuracy that isn't meaningfully better.

## Urgency

### Accuracy (test macro-F1)

| Model | Macro-F1 |
|---|---|
| baseline (`baseline_linear_svc_v1`) | 0.7944 |
| transformer (`transformer_deberta-v3-small_v1`) | 0.9095 |
| transformer (`transformer_distilbert-base-uncased_v1`) | 0.9074 |

### Latency (CPU, single request, p50/p95)

| Model | p50 ms | p95 ms |
|---|---|---|
| baseline | 1.6 | 1.7 |
| deberta-v3-small | 29.4 | 31.8 |
| distilbert-base-uncased | 12.1 | 13.7 |

### Export size

| Model | Size |
|---|---|
| baseline | 3.5 MB |
| deberta-v3-small | 551.9 MB |
| distilbert-base-uncased | 256.3 MB |

### Recommendation

**Deploy the transformer** (`transformer_deberta-v3-small_v1`). Macro-F1 delta over the baseline is **+0.1151**, above the 0.02 margin treated as a real win here — worth the 159x size and 18.0x latency cost.
